# Compatibilidad para instalación mínima no bloqueada.
# Para un entorno reproducible usa el lock de tu versión de Python:
#   pip install -r locks/py3.11.txt
#   pip install -r locks/py3.12.txt
#   pip install -r locks/py3.13.txt
-r requirements/core.in
